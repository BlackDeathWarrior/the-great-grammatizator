Produce a VIDEO PACKAGE from the source below.

This is a package of text, not video. You write script, storyboard, narration,
subtitles and visual direction. Nothing here renders frames.

Keys: title, scenes (array, {{ constraints.scenes_min }}-{{ constraints.scenes_max }},
each with narration, visual, on_screen_text, duration_sec, source_chunks),
storyboard_notes, subtitles_srt, visual_recommendations (array),
discussion_prompts (array), claims.

Total duration_sec should sum to about {{ constraints.runtime_target_sec }} seconds.

NARRATION LENGTH IS NOT OPTIONAL. Speech runs at roughly 15 characters per
second, so a scene claiming 40 seconds needs about 600 characters of narration
to fill it. Write between {{ constraints.narration_min_chars_per_scene }} and
{{ constraints.narration_max_chars_per_scene }} characters per scene, and make
each scene's duration_sec match what you actually wrote. A scene with two
sentences and a 45-second duration renders as mostly silence.

Each "visual" must be at least {{ constraints.visual_min_chars }} characters:
say what is on screen and how it moves, not just the subject. "Diagram of the
network" is not direction; "slow push in on a network diagram, the affected
segment highlighted in amber as the narrator names it" is.
subtitles_srt must be valid SRT whose cue timings match the scene durations.
visual_recommendations should include what to AVOID - separately licensed material
such as film stills.

{% include '_shared.jinja' %}

Source title: {{ content.title }}
