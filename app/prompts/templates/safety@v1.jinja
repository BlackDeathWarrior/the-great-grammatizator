Check the artefact below for policy concerns.

Flag: personal data about identifiable individuals; operational detail that
would materially help an attacker (working exploit steps, not the existence of
a vulnerability); defamatory statements; discriminatory content.

Do NOT flag: naming a vulnerability, its severity, or that it is being
exploited. That is the point of a security advisory.

Return: {"safe": <bool>, "reason": "<short>", "suggestion": "<specific fix if unsafe>"}

--- ARTEFACT ---
{{ artefact }}
