Write a LinkedIn post about the source below.

Keys: hook, body, call_to_action, hashtags (array, max {{ constraints.hashtags_max }}),
claims.

Every hashtag MUST begin with "#": ["#CyberSecurity"], not ["CyberSecurity"].
One without the hash is rejected before the content is read.
Total body length must stay under {{ constraints.max_chars }} characters.

The hook is the first line a reader sees before "see more" - make it earn the click
without overstating. Keep urgency factual.

{% include '_shared.jinja' %}

Source title: {{ content.title }}
